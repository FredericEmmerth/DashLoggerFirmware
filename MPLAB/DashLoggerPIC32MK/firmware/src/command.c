#include "command.h"

void COMMAND_Generate(signals_signal* signal_list, uint32_t signal_list_len, 
        SHORTPROTOCOL_Instance* inst){
    
    if(SHORTPROTOCOL_Available(inst)){
        
    }
    
}

void COMMAND_ChangeString(uint32_t ID, uint8_t* newString,
        uint32_t newStringLength){
    
}
